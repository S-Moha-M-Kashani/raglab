"""Scoring what the stages produced: the deterministic metrics, the RAGAS
judged metrics, the run harness, the shipped-assistant baseline preset, and
evaluation's own service — the experiment ledger."""
