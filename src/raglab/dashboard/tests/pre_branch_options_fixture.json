{"chunkers":["semantic-drift","fixed","fixed-overlap","message","turn-pair","session"],"embedders":["sentence-transformers","fastembed","ascii-hash","token-hash","char-hash"],"retrievers":["hybrid-rrf","dense","bm25"],"rerankers":["lexical","none","recency","agentic","cross-encoder","llm"],"graders":["none","lexical","llm"],"answerers":["extractive","none","llm"],"hierarchies":["","louvain","leiden","label-prop","raptor","agglomerative","kmeans","metadata"],"graph_sources":["hybrid","knn","lexical","bipartite-terms"],"summarizers":["centroid","lead-idf","mmr","card"],"summary_scopes":["mixed","leaves","summaries","drill-down"],"hierarchy_support":{"leiden":{"available":true,"install":"uv sync --extra graph-index"},"louvain":{"available":true,"install":"nothing — networkx is a core dependency"},"label-prop":{"available":true,"install":"nothing — networkx is a core dependency"},"raptor":{"available":true,"install":"nothing — scikit-learn is a core dependency"},"agglomerative":{"available":true,"install":"nothing — scikit-learn is a core dependency"},"kmeans":{"available":true,"install":"nothing — scikit-learn is a core dependency"},"metadata":{"available":true,"install":"nothing — it reads the corpus"}},"question_labels":{"question_type":["single-hop","temporal","multi-hop","aggregation","knowledge-update","commitment","entity","pattern","abstention","adversarial","habit"],"difficulty":["easy","medium","hard"],"query_threads":["jacqueline-fights","job-search","turing-college","failed-ambitions","apartment","money-investing","tax-problem","neighbor-noise","mother-health","articles-research","work-stress","side-project","sleep-health","travel-dream","family-gatherings","car-trouble","good-days","habit-tracking"]},"balances":["","difficulty","query_threads","question_type"],"dependencies":{"index.chunk_chars":{"field":"index.chunker","on":["semantic-drift","fixed","fixed-overlap"],"reason":"the message, turn-pair and session chunkers cut on structure, not on length"},"index.overlap":{"field":"index.chunker","on":["fixed-overlap"],"reason":"only the fixed-overlap chunker slides a window"},"index.embed_model":{"field":"index.embedder","on":["fastembed","sentence-transformers"],"reason":"the hash embedders load no model"},"index.graph_source":{"field":"index.hierarchy","on":["louvain","leiden","label-prop"],"reason":"the embedding clusterings and the metadata grouping build no graph"},"index.graph_knn":{"field":"index.graph_source","on":["hybrid","knn"],"reason":"this edge source builds no nearest-neighbour edges"},"index.granularity":{"field":"index.hierarchy","on":["louvain","leiden","raptor","agglomerative","kmeans"],"reason":"label propagation has no granularity parameter — that is what makes it the control — and the metadata groups are given rather than chosen"},"index.hierarchy_levels":{"field":"index.hierarchy","on":["raptor","agglomerative","louvain","leiden"],"reason":"this grouping produces one level and stops"},"index.min_group":{"field":"index.hierarchy","on":["louvain","leiden","label-prop","raptor","agglomerative","kmeans","metadata"],"reason":"nothing is grouped"},"index.summarizer":{"field":"index.hierarchy","on":["louvain","leiden","label-prop","raptor","agglomerative","kmeans","metadata"],"reason":"nothing is grouped, so nothing is summarised"},"retrieval.summary_scope":{"field":"index.hierarchy","on":["louvain","leiden","label-prop","raptor","agglomerative","kmeans","metadata"],"reason":"this index is flat — it holds no summaries to scope"},"retrieval.summary_boost":{"field":"index.hierarchy","on":["louvain","leiden","label-prop","raptor","agglomerative","kmeans","metadata"],"reason":"this index is flat — there is nothing to boost"},"retrieval.summary_levels":{"field":"index.hierarchy","on":["raptor","agglomerative","louvain","leiden"],"reason":"only a grouping with more than one level has levels to choose between"},"retrieval.rrf_k":{"field":"retrieval.retriever","on":["hybrid-rrf"],"reason":"only hybrid-rrf fuses two rankings"},"retrieval.rerank_depth":{"field":"retrieval.reranker","on":["lexical","recency","agentic","cross-encoder","llm"],"reason":"nothing is reranked"},"retrieval.reranker_model":{"field":"retrieval.reranker","on":["llm"],"reason":"only the llm reranker calls a model"},"retrieval.time_filter":{"field":"dataset.date_label","on_true":true,"reason":"this corpus declares no date label"},"retrieval.recency_half_life_days":{"field":"retrieval.reranker","on":["recency","agentic"],"reason":"only the recency and agentic rerankers weigh age","also":{"field":"dataset.date_label","on_true":true,"reason":"this corpus declares no date label"}},"retrieval.agentic_weights":{"field":"retrieval.reranker","on":["agentic"],"reason":"only the agentic reranker has weights to balance","also":{"field":"dataset.ranks_label","on_true":true,"reason":"this corpus declares no ranks label"}},"retrieval.grade_threshold":{"field":"retrieval.grader","on":["lexical","llm"],"reason":"the gate is off, so nothing is scored to threshold"},"retrieval.grader_model":{"field":"retrieval.grader","on":["llm"],"reason":"only the llm gate calls a model"},"retrieval.expansion_model":{"field":"retrieval.hyde","on_true":true,"reason":"HyDE is off — multi-query expansion is rule-based and uses no model"},"generation.model":{"field":"generation.answerer","on":["llm"],"reason":"only the llm answerer calls a model"},"generation.judge_model":{"field":"generation.fact_judge","on_true":true,"reason":"the fact judge is off"}},"defaults":{"index":{"dataset":"","chunker":"semantic-drift","chunk_chars":500,"overlap":100,"contextual":true,"embedder":"sentence-transformers","embed_model":"","hierarchy":"","graph_source":"hybrid","graph_knn":8,"granularity":1.0,"hierarchy_levels":1,"min_group":3,"summarizer":"centroid"},"retrieval":{"retriever":"hybrid-rrf","k":8,"candidates":40,"rrf_k":60,"time_filter":true,"multi_query":true,"hyde":false,"expansion_model":"","mmr_lambda":1.0,"reranker":"lexical","rerank_depth":20,"reranker_model":"","recency_half_life_days":180.0,"agentic_weights":[1.0,0.3,0.2],"grader":"none","grade_threshold":0.0,"grader_model":"","max_context_chars":6000,"summary_scope":"mixed","summary_boost":1.0,"summary_levels":""},"generation":{"answerer":"extractive","model":"","fact_judge":false,"judge_model":"","ragas_model":""},"label":""},"steps":[{"key":"index","short":"Index","label":"Index — what gets stored","note":"Runs once per corpus. It decides what can ever be found, so a change here rebuilds the collection and invalidates the numbers below it."},{"key":"retrieval","short":"Retrieval","label":"Retrieval & ranking","note":"Runs on every question against an index that already exists — which is why these are the knobs worth sweeping first."},{"key":"generation","short":"Generation","label":"Generation & scoring","note":"Turns the retrieved contexts into a Farsi answer, refuses when the diary is silent, and grades what it wrote."}],"embedder_hints":[{"kind":"sentence-transformers","label":"sentence-transformers — any HuggingFace model","languages":"English + Farsi — depends on the model below","farsi":true,"note":"The lab default, because it is the only backend that can load an arbitrary HuggingFace checkpoint — which is how you reach an encoder tuned for one language, such as the Persian one the bundled diary needs and fastembed does not serve. Needs the local-embeddings extra and downloads the weights once.","available":true},{"kind":"fastembed","label":"fastembed — a real transformer","languages":"English + Farsi — depends on the model below","farsi":true,"note":"Embeds meaning rather than letters, from its own short ONNX list; 0.612 session recall measured on the bundled diary. Which languages it covers is decided by the model below — pick one that covers your corpus, or this is an English model reading text it cannot read.","available":true},{"kind":"ascii-hash","label":"ascii-hash — the reference baseline","languages":"Latin script only (a–z, 0–9)","farsi":false,"note":"tokenises [a-z0-9]+, so a non-Latin sentence has no tokens at all and every vector is zero — 0.014 session recall on the bundled diary, i.e. chance. Kept as the baseline that makes the point, not as a working option.","available":true},{"kind":"token-hash","label":"token-hash — hashed Persian words","languages":"any script, lexical only (no meaning)","farsi":true,"note":"Unicode-aware bag of normalised words. Sees any script but not the meaning, so two inflections of one word are two unrelated tokens and a paraphrase is invisible.","available":true},{"kind":"char-hash","label":"char-hash — hashed character 4-grams","languages":"any script, lexical only (no meaning)","farsi":true,"note":"Character n-grams recover a stem across its affixes, which is why it is the strongest option that downloads nothing: 0.386 recall against ascii-hash's 0.014 on the bundled diary. Still lexical — a paraphrase sharing no letters is invisible to it.","available":true}],"embed_models":[{"id":"","label":"the backend's own default","languages":"English + Farsi — depends on the model below","farsi":true,"source":"default","dim":0,"available":true,"backend":"","tag":"","note":"sentence-transformers → persian-embeddings; fastembed → paraphrase-multilingual-MiniLM-L12-v2 (RAGLAB_FASTEMBED_MODEL)","query_prefix":"","passage_prefix":""},{"id":"heydariAI/persian-embeddings","label":"persian-embeddings (heydariAI)","languages":"Farsi + English (Persian-tuned)","farsi":true,"source":"open","dim":1024,"note":"the lab default: fine-tuned on Persian specifically rather than multilingual-by-accident, ~2.2 GB, and the cheapest real encoder to try on this corpus. Loaded through sentence-transformers, which is what its model card recommends. ~512-token context.","available":true,"backend":"sentence-transformers","tag":"lab default","query_prefix":"","passage_prefix":""},{"id":"intfloat/multilingual-e5-small","label":"multilingual-e5-small","languages":"English + Farsi (100+ languages)","farsi":true,"source":"open","dim":384,"note":"the e5 retrieval recipe at a fifth of e5-large's size, and its weights are already on disk here. Served through sentence-transformers — this fastembed does not carry it — and it needs its query/passage prefixes to perform, which the lab applies for you.","available":true,"backend":"sentence-transformers","tag":"","query_prefix":"query: ","passage_prefix":"passage: "},{"id":"sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2","label":"paraphrase-multilingual-MiniLM-L12-v2","languages":"English + Farsi (50+ languages)","farsi":true,"source":"open","dim":384,"note":"the fastembed default: smallest multilingual option, ~120 MB, 384 dims. Fine as a floor, and the weakest of the Farsi-capable models.","available":true,"backend":"fastembed","tag":"","query_prefix":"","passage_prefix":""},{"id":"BAAI/bge-small-en-v1.5","label":"bge-small-en-v1.5","languages":"English only","farsi":false,"source":"open","dim":384,"note":"what the brain hardwires today. Here as the baseline: it will return confident numbers that mean nothing on Farsi text.","available":true,"backend":"fastembed","tag":"","query_prefix":"","passage_prefix":""},{"id":"sentence-transformers/all-MiniLM-L6-v2","label":"all-MiniLM-L6-v2","languages":"English only","farsi":false,"source":"open","dim":384,"note":"the most-copied embedder on the internet, and the wrong one for this corpus.","available":true,"backend":"fastembed","tag":"","query_prefix":"","passage_prefix":""}],"models":[{"id":"","label":"lab default (4skl/gemma4-e2b-mtp)","source":"default","available":true,"note":"follows RAGLAB_MODEL, so one change moves every stage"},{"id":"gemma4:e2b","label":"Gemma 4 E2B","source":"open","note":"read the Farsi correctly and scored 4/5 on the judge screen; ~17s per judged call, because it reasons before every verdict","available":true},{"id":"qwen3.5:2b","label":"Qwen3.5 2B","source":"open","note":"the speed candidate for the judge — Qwen is the stronger multilingual family at this size, but the two nearest-size models screened so far were both constant predictors, so screen it before trusting a row it produced","available":true},{"id":"4skl/gemma4-e2b-mtp","label":"Gemma 4 E2B (MTP)","source":"open","note":"multi-token-prediction build of the above, and 3.7 GB at Q4_0 against 7.2 GB at Q4_K_M — so it is a throughput test, and the quantisation differs too, which means a quality difference from the base build is not ruled out","available":true},{"id":"deepseek-r1:8b","label":"DeepSeek-R1 8B","source":"open","note":"the strongest reasoner installed at a size that still answers, but reasoning is the cost here: the judge is the high-volume stage, and a model that spends hundreds of thinking tokens per verdict pays that on every call","available":true},{"id":"dolphin-mixtral:8x7b","label":"Dolphin Mixtral 8x7B","source":"open","note":"the largest thing installed; a 47B mixture is slow enough that it is an answerer, not a 276-call judge","available":true},{"id":"llama3.1:8b","label":"Llama 3.1 8B","source":"open","note":"read the Farsi but was a constant predictor as a judge — usable to answer, never to grade","available":false}],"model_roles":[{"key":"expand","label":"Query rewriting (HyDE)","field":"retrieval.expansion_model","help":"Writes a hypothetical answer and searches with that instead of the question, on the theory that an answer looks more like the text you are hunting for than a question does. Only HyDE uses a model; multi-query expansion is rule-based and free. Dataset-specific: written for the bundled Farsi diary, not portable to a corpus in another language. It stays in the lab as the worked example of a general rule — a filter that knows its corpus's language and metadata beats a language-neutral pipeline. The prompt asks in Persian for a diary-style paragraph, so elsewhere it searches with text in the wrong language rather than going quiet.","only_when":"HyDE is on","step":"retrieval"},{"key":"rerank","label":"Reranker","field":"retrieval.reranker_model","help":"Reads the top candidates and scores each one against the question. It sits in the latency path of every query, so a slow model here is felt on every single question.","only_when":"Reranker = llm","step":"retrieval"},{"key":"grade","label":"Relevance gate","field":"retrieval.grader_model","help":"Decides whether a chunk is relevant at all. This is what lets the lab abstain rather than answer from noise: measured on the bundled diary, an LLM gate at 0.4 refused all five unanswerable questions while wrongly refusing 3% of the answerable ones — the lexical gate had no threshold that could do both.","only_when":"Gate = llm","step":"retrieval"},{"key":"answer","label":"Answer","field":"generation.model","help":"Writes the answer from the retrieved context, cites session ids, and is the stage that must refuse when the corpus is silent. On the bundled diary generation is the bottleneck — faithfulness 0.743 against fact coverage 0.261 — so this is the interesting dropdown.","only_when":"Answerer = llm","step":"generation"},{"key":"judge","label":"Fact judge","field":"generation.judge_model","help":"Checks an answer against the ground truth's atomic derived_facts. When the two are not in the same language — the bundled diary answers in Farsi against English facts — it is translating as well as judging, which is why no deterministic metric can replace it, and why a weak model here produces confidently wrong scores.","only_when":"the fact judge is on","step":"generation"},{"key":"ragas","label":"RAGAS judge","field":"generation.ragas_model","help":"The model RAGAS uses for faithfulness, answer relevancy and factual correctness. Separate from the answerer on purpose: a model grading its own output is not evidence.","only_when":"RAGAS = judged","step":"generation"}],"modes":[{"key":"local","label":"Local (Ollama)","provider":"ollama","note":"every LLM stage on a model on this machine — free and private — with the lab's own stage defaults: extractive answerer, lexical reranker, no gate","config":{"retrieval":{"hyde":false,"expansion_model":"","reranker":"lexical","reranker_model":"","grader":"none","grader_model":"","grade_threshold":0.0},"generation":{"answerer":"extractive","model":"","fact_judge":false,"judge_model":"","ragas_model":""}},"models":[{"id":"","label":"lab default (4skl/gemma4-e2b-mtp)","source":"default","available":true,"note":"follows RAGLAB_MODEL, so one change moves every stage"},{"id":"gemma4:e2b","label":"Gemma 4 E2B","source":"open","note":"read the Farsi correctly and scored 4/5 on the judge screen; ~17s per judged call, because it reasons before every verdict","available":true},{"id":"qwen3.5:2b","label":"Qwen3.5 2B","source":"open","note":"the speed candidate for the judge — Qwen is the stronger multilingual family at this size, but the two nearest-size models screened so far were both constant predictors, so screen it before trusting a row it produced","available":true},{"id":"4skl/gemma4-e2b-mtp","label":"Gemma 4 E2B (MTP)","source":"open","note":"multi-token-prediction build of the above, and 3.7 GB at Q4_0 against 7.2 GB at Q4_K_M — so it is a throughput test, and the quantisation differs too, which means a quality difference from the base build is not ruled out","available":true},{"id":"deepseek-r1:8b","label":"DeepSeek-R1 8B","source":"open","note":"the strongest reasoner installed at a size that still answers, but reasoning is the cost here: the judge is the high-volume stage, and a model that spends hundreds of thinking tokens per verdict pays that on every call","available":true},{"id":"dolphin-mixtral:8x7b","label":"Dolphin Mixtral 8x7B","source":"open","note":"the largest thing installed; a 47B mixture is slow enough that it is an answerer, not a 276-call judge","available":true},{"id":"llama3.1:8b","label":"Llama 3.1 8B","source":"open","note":"read the Farsi but was a constant predictor as a judge — usable to answer, never to grade","available":false}]},{"key":"openrouter","label":"OpenRouter","provider":"openrouter","note":"the full LLM pipeline on gpt-5-nano — HyDE, LLM reranker, relevance gate, answerer and both judges. No mode touches the index, so the embedder is left exactly where you set it","config":{"retrieval":{"hyde":true,"expansion_model":"openai/gpt-5-nano","reranker":"llm","reranker_model":"openai/gpt-5-nano","grader":"llm","grader_model":"openai/gpt-5-nano","grade_threshold":0.4},"generation":{"answerer":"llm","model":"openai/gpt-5-nano","fact_judge":true,"judge_model":"openai/gpt-5-nano","ragas_model":"openai/gpt-5-nano"}},"models":[{"id":"","label":"lab default (openai/gpt-5-nano)","source":"default","available":true,"note":"follows RAGLAB_MODEL, so one change moves every stage"},{"id":"openai/gpt-5-nano","label":"GPT-5 nano","source":"closed","note":"every grade in .runs/ so far was measured on this","available":true},{"id":"openai/gpt-5-mini","label":"GPT-5 mini","source":"closed","note":"the same family with more capacity — the obvious A/B","available":true},{"id":"anthropic/claude-haiku-4.5","label":"Claude Haiku 4.5","source":"closed","note":"fast, and strong at \"answer only from this context\"","available":true},{"id":"google/gemini-2.5-flash","label":"Gemini 2.5 Flash","source":"closed","note":"cheap long context, useful for the summary pass","available":true}]},{"key":"claude","label":"Claude (CLI)","provider":"claude","note":"the full LLM pipeline on the Claude Code CLI already logged in on this machine, so no API key is needed at all — HyDE, LLM reranker, relevance gate, answerer and both judges. At effort=low a call cost 3.9s on a short probe and 5.6–7.4s on the prompts the lab actually sends, and the calls bill your Claude account rather than nothing","config":{"retrieval":{"hyde":true,"expansion_model":"sonnet","reranker":"llm","reranker_model":"sonnet","grader":"llm","grader_model":"sonnet","grade_threshold":0.4},"generation":{"answerer":"llm","model":"sonnet","fact_judge":true,"judge_model":"sonnet","ragas_model":"sonnet"}},"models":[{"id":"","label":"lab default (sonnet)","source":"default","available":true,"note":"follows RAGLAB_MODEL, so one change moves every stage"},{"id":"sonnet","label":"Claude Sonnet (CLI)","source":"closed","note":"what this backend was measured on, all at effort=low: 3.9s per call on a short grade probe, 5.6s on the lab's real grade prompt, and ~7.4s per call-slot across a judged run — a longer prompt costs more, so the probe figure is the floor rather than the price","available":true},{"id":"opus","label":"Claude Opus (CLI)","source":"closed","note":"the sweep's judge under this backend — a model grading its own output is not evidence, so the answerer stays sonnet","available":true},{"id":"haiku","label":"Claude Haiku (CLI)","source":"closed","note":"the speed candidate: the judge is ~420 calls per run, and each one here is a process spawn as well as a turn","available":true}]},{"key":"codex","label":"Codex (CLI)","provider":"codex","note":"the same full pipeline on the Codex CLI. ~8.2s per call at effort=low, and every call carries codex's own ~18.5k-token agent preamble, which no flag removes","config":{"retrieval":{"hyde":true,"expansion_model":"gpt-5.6-luna","reranker":"llm","reranker_model":"gpt-5.6-luna","grader":"llm","grader_model":"gpt-5.6-luna","grade_threshold":0.4},"generation":{"answerer":"llm","model":"gpt-5.6-luna","fact_judge":true,"judge_model":"gpt-5.6-luna","ragas_model":"gpt-5.6-luna"}},"models":[{"id":"","label":"lab default (gpt-5.6-luna)","source":"default","available":true,"note":"follows RAGLAB_MODEL, so one change moves every stage"},{"id":"gpt-5.6-luna","label":"GPT-5.6 Luna (Codex CLI)","source":"closed","note":"the cost-sensitive default for high-volume judging, run at low reasoning effort","available":true},{"id":"gpt-5.6-terra","label":"GPT-5.6 Terra (Codex CLI)","source":"closed","note":"8.2s per call at effort=low, and every call carries codex's own ~18.5k-token agent preamble, which no flag removes","available":true}]}],"metrics":[{"key":"headline","label":"Composite","short":"weighted retrieval score","step":"","formula":"0.4·recall + 0.3·quote_recall + 0.2·ndcg + 0.1·abstained_correctly, renormalised over whichever of the four this run produced","library":"metrics._headline — pure Python, no model, so it never varies between runs","help":"One comparable number for the leaderboard, weighted in the order that matters: did retrieval find the evidence, did the answering sentence survive chunking, was the evidence ranked first, and was an unanswerable question refused. Generation quality is deliberately excluded, so a config measured with the extractive answerer stays comparable to one measured with an LLM."},{"key":"recall","label":"Recall@k","short":"evidence sessions found","step":"retrieval","formula":"|gold ∩ top-k| / |gold|","library":"metrics.recall_at_k — pure Python, no model, so it never varies between runs","help":"Of the sessions the ground truth marks as evidence, the share that appear in the top k retrieved. This is the ceiling on everything downstream: an answer cannot cite what retrieval never returned. Questions with no evidence (the unanswerable ones) are excluded rather than scored zero."},{"key":"quote_recall","label":"Quote recall","short":"answering sentence survived chunking","step":"retrieval","formula":"matched quotes / total quotes, where a quote counts as matched if it is a substring of the normalised context or its longest common run covers >= 0.9 of it","library":"metrics.quote_recall (difflib.SequenceMatcher) — pure Python, no model, so it never varies between runs","help":"Session recall says the right session was found; this says the sentence that actually answers the question is inside the retrieved text. A splitter that cuts mid-thought scores well on the first and badly on this one, which is exactly the failure a session-level metric hides."},{"key":"ndcg","label":"nDCG@k","short":"evidence ranked first","step":"retrieval","formula":"DCG/IDCG with binary gains: DCG = Σ gain_i / log2(i+2), IDCG the same sum over a perfect ordering","library":"metrics.ndcg_at_k (math.log2) — pure Python, no model, so it never varies between runs","help":"Rewards putting evidence at the top rather than merely including it somewhere in k. It matters because the answerer sees a context that gets truncated, so rank 1 and rank 8 are not worth the same."},{"key":"mrr","label":"MRR","short":"rank of first evidence","step":"retrieval","formula":"1 / rank of the first evidence session, 0 if none was retrieved","library":"metrics.mrr — pure Python, no model, so it never varies between runs","help":"How deep you have to read before the first correct hit. Sensitive to the top of the list only — useful next to nDCG, not instead of it."},{"key":"precision","label":"Precision@k","short":"signal to noise","step":"retrieval","formula":"|top-k ∩ gold| / |top-k|","library":"metrics.precision_at_k — pure Python, no model, so it never varies between runs","help":"How much of what was retrieved is actually evidence. Low precision is not automatically bad — it is the cost of a wide k — but it is what dilutes the answerer's context and what MMR trades against."},{"key":"hit","label":"Hit@k","short":"at least one evidence session","step":"retrieval","formula":"1 if top-k contains any gold session, else 0","library":"metrics.hit_at_k — pure Python, no model, so it never varies between runs","help":"The most forgiving retrieval metric: did anything relevant come back at all. Useful for spotting total misses that an averaged recall softens."},{"key":"abstained_correctly","label":"Abstention","short":"unanswerable refused","step":"generation","formula":"refusals / unanswerable questions","library":"metrics.score_question, reading the answerer's refusal flag — pure Python, no model, so it never varies between runs","help":"The corpus genuinely has nothing about some of these questions. Saying so is the correct answer, and this is the fraction of those where the answerer refused instead of inventing something."},{"key":"false_abstention","label":"False refusals","short":"answerable wrongly refused","step":"generation","formula":"refusals / answerable questions (lower is better)","library":"metrics.score_question, reading the answerer's refusal flag — pure Python, no model, so it never varies between runs","help":"The other side of abstention, and the failure a badly tuned relevance gate produces: the evidence was there and the answerer still said it had nothing. Read it together with abstention — a gate that refuses everything scores perfectly on one and terribly here."},{"key":"answer_similarity","label":"Answer similarity","short":"vs the reference answer","step":"generation","formula":"difflib ratio = 2·M / T over normalised characters, where M is matched characters and T the combined length","library":"metrics.answer_similarity (difflib.SequenceMatcher) — pure Python, no model, so it never varies between runs","help":"A blunt instrument, chosen because it is a *stable* one: no model, no variance. In practice it mostly tracks whether the same names, dates and numbers appear, and it punishes a correct short answer — which is why token F1 sits beside it. It compares characters, so it is meaningless when the answer and the reference are in different languages."},{"key":"answer_token_f1","label":"Answer token F1","short":"unigram overlap","step":"generation","formula":"F1 = 2PR/(P+R) over content words, P = overlap/|predicted|, R = overlap/|reference|, counting duplicates once","library":"metrics.token_f1 (textnorm tokeniser) — pure Python, no model, so it never varies between runs","help":"The SQuAD-style measure. It credits a short correct answer that a character-similarity ratio penalises purely for being short."},{"key":"fact_coverage","label":"Derived facts","short":"judged fact coverage","step":"generation","formula":"facts the judge marked present / derived_facts in the ground truth","library":"evaluate.judge_derived_facts — an LLM judge (the \"Fact judge\" model role), so this number carries that model's variance","help":"The ground truth breaks its reference answer into atomic derived_facts, and a judge checks each one against the produced answer. When the facts and the answer are in different languages — the bundled diary lists English facts against Farsi answers — the judge is translating as well as checking, which is why no deterministic metric replaces it, and why a weak model here produces confidently wrong scores."},{"key":"latency_ms","label":"Latency","short":"ms per question","step":"","formula":"sum of the per-stage timings for one question, in milliseconds","library":"time.perf_counter around each pipeline stage — pure Python, no model, so it never varies between runs","help":"Whole-pipeline wall clock, so it spans every stage rather than grading one. Dominated by whatever calls a model: an LLM reranker or gate moves this by orders of magnitude, a hash embedder does not."},{"key":"n_contexts","label":"Contexts","short":"chunks handed to the answerer","step":"retrieval","formula":"mean number of contexts surviving rerank, gate and expansion","library":"metrics.score_question — pure Python, no model, so it never varies between runs","help":"What k actually delivered. It falls below k when the relevance gate removes chunks, which is the honest reason an answer had less to work with."},{"key":"context_chars","label":"Context size","short":"characters of context","step":"retrieval","formula":"mean total characters of the assembled context","library":"metrics.score_question — pure Python, no model, so it never varies between runs","help":"How much text the answerer was given. Worth watching next to precision: two configs with the same recall can differ by 3x here, and the bigger one is paying for it in tokens and in dilution."},{"key":"n_summaries","label":"Summaries used","short":"summary rows in the context","step":"index","formula":"mean count of contexts whose layer is summary","library":"metrics.score_question — pure Python, no model, so it never varies between runs","help":"How often the summary hierarchy was actually retrieved. This is the number that had to exist: a hierarchy scoring flat because nothing retrieved it and a hierarchy scoring flat because it did not help are different findings, and no other field on the row tells them apart. Zero here means the second reading is unavailable — which is exactly what the lab found once, having deleted five summary layers that were correct, reachable, and almost never retrieved."},{"key":"n_expanded","label":"Drilled down","short":"members reached through a summary","step":"retrieval","formula":"mean count of contexts expanded from a retrieved summary","library":"metrics.score_question — pure Python, no model, so it never varies between runs","help":"Contexts that arrived because a summary was retrieved first and then expanded to the chunks it stands for — the drill-down scope. Zero under any other scope, by construction. Read beside Summaries used: the summary is what states an aggregate, and these are the leaves the answerer can actually quote."},{"key":"ragas_decision","label":"RAGAS decision score","short":"the number the architecture was chosen by","step":"","formula":"mean(faithfulness, answer_relevancy, llm_context_precision_with_reference, context_recall) — undefined unless all four are present","library":"ragas_eval.decision_score over ragas 0.4.x metrics, scored by the RAGAS judge model — a model's verdict, so it varies","help":"Four judged RAGAS metrics, averaged unweighted, and the only score that picks between configurations here. They were chosen because between them they cover both halves of the pipeline in both directions: context precision asks whether what came back was relevant and well ranked, context recall whether everything the answer needed was retrieved, faithfulness whether the answer stayed inside what was retrieved, and answer relevancy whether it addressed the question at all. Everything else on this screen is reported and none of it votes — factual correctness grades the fixture's phrasing as much as the pipeline, the offline context pair punish large chunks for being large, and our own deterministic metrics grade retrieval almost exclusively, so ranking on them rewards a config that finds the evidence and then says nothing useful about it. The cost of that choice is honest: all four come from a judge, so this number carries a model's variance, and it is only comparable between runs scored by the same RAGAS judge model."},{"key":"non_llm_context_recall","label":"Context recall (offline)","short":"RAGAS, string distance","step":"retrieval","formula":"reference quotes matched in the retrieved contexts / reference quotes, matching by string similarity","library":"ragas 0.4.x NonLLMContextRecall — string distance via rapidfuzz, no model involved","help":"RAGAS's context recall with the judge removed: it compares the retrieved context to the ground truth's verbatim evidence quotes as strings. Deterministic, and the RAGAS number to compare configs on — but it compares *whole strings*, so longer chunks score lower even when the answer is in them. Use quote recall to compare across chunkers."},{"key":"non_llm_context_precision_with_reference","label":"Context precision (offline)","short":"RAGAS, string distance","step":"retrieval","formula":"mean precision@k over the retrieved contexts, a context counting as relevant when it is similar enough to a reference quote","library":"ragas 0.4.x NonLLMContextPrecisionWithReference — string distance via rapidfuzz, no model involved","help":"How much of the retrieved context matches the reference evidence, weighted towards the top of the ranking. Same whole-string caveat as its recall twin."},{"key":"faithfulness","label":"Faithfulness","short":"answer supported by the context","step":"generation","formula":"supported claims / total claims in the answer","library":"ragas 0.4.x Faithfulness, scored by the RAGAS judge model — a model's verdict, so it varies","help":"RAGAS's own definition: the judge breaks the generated answer into individual claims, then checks each one against the retrieved context. An answer is faithful when the context supports every claim in it — so this measures invention, not correctness. A perfectly faithful answer can still be wrong if retrieval returned the wrong passage."},{"key":"answer_relevancy","label":"Answer relevancy","short":"does it answer the question","step":"generation","formula":"mean cosine similarity between the original question and n questions the judge reverse-engineers from the answer","library":"ragas 0.4.x ResponseRelevancy (judge + the lab's own embedder for the vectors), scored by the RAGAS judge model — a model's verdict, so it varies","help":"RAGAS asks the judge to invent the questions the answer would be a reply to, embeds them, and compares them with the real question. It catches answers that are true and on-topic but do not actually address what was asked. The vectors come from the embedder under test, so no external embedding API is called."},{"key":"factual_correctness(mode=f1)","label":"Factual correctness","short":"claims vs the reference answer","step":"generation","formula":"F1 = 2PR/(P+R) over claims: P = claims in the answer supported by the reference, R = claims in the reference present in the answer","library":"ragas 0.4.x FactualCorrectness(mode=f1), scored by the RAGAS judge model — a model's verdict, so it varies","help":"Unlike faithfulness, this compares the answer to the *reference answer* rather than to the retrieved context — so it measures being right, not merely being grounded. Both directions are counted, which is why it is an F1: an answer that omits half the reference loses recall, one that adds unsupported detail loses precision."},{"key":"llm_context_precision_with_reference","label":"Context precision (judged)","short":"RAGAS, judged relevance","step":"retrieval","formula":"mean precision@k where the judge, not a string metric, decides whether each retrieved context is relevant","library":"ragas 0.4.x LLMContextPrecisionWithReference, scored by the RAGAS judge model — a model's verdict, so it varies","help":"The judged twin of the offline precision metric. It is immune to the whole-string penalty that makes the offline pair unfair to large chunks, at the cost of a model call per context and a model's variance."},{"key":"context_recall","label":"Context recall (judged)","short":"RAGAS, judged coverage","step":"retrieval","formula":"reference claims attributable to the retrieved context / reference claims","library":"ragas 0.4.x LLMContextRecall, scored by the RAGAS judge model — a model's verdict, so it varies","help":"The judge splits the reference answer into claims and asks, for each one, whether the retrieved context could support it. It answers \"was everything needed retrieved?\" without depending on the exact wording of a quote."}],"help":{"index.dataset":"Which corpus this experiment measures against. It is the first thing the leaderboard groups by, because two corpora are two measurements rather than two configurations of one, and changing it rebuilds the index. The bundled default is a synthetic Farsi diary with 112 ground-truth questions; four smaller corpora ship beside it so a general finding can be told from a fact about Farsi diaries. Import your own with the button beside it — the ! there states the shape a file must have — and a dataset whose evidence quotes are not verbatim in the messages they cite is refused.","run.dataset-file":"A dataset is two JSON files, paired by id: a corpus and the ground truth measured against it. Both are checked in full against fixtures/corpus_groundtruth_datasets/schema_corpus.json and schema_groundtruth.json and refused — never repaired — with every problem reported at once.\n\nThe corpus: \"corpus_dataset_metadata\": {\"dataset\": \"support-tickets\", \"name\": \"…\", \"language\": \"en\"} — dataset is the id every run and leaderboard row records, 2+ characters, never a filename. \"corpus_documents\": [{\"corpus_document_id\": 1, \"document_content\": [{\"text\": \"…\"}]}] — one entry per document, one part per turn; a label used anywhere (on a document, a part, a question) must be declared in label_fields, which states its type, which levels it may apply to, and — for a closed set — its allowed values. One label typed \"date-time\" drives time filtering and recency; one numeric label declaring ranks:true drives importance; neither is required.\n\nThe ground truth: \"groundtruth_dataset_metadata\": {\"name\": \"…\", \"corpus_ref\": {\"dataset\": \"support-tickets\"}} — corpus_ref.dataset must name the corpus above. \"groundtruth_dataset\": [{\"groundtruth_question_id\": 1, \"question\": \"…\", \"expected_answer\": {\"behavior\": \"answer\", \"text\": \"…\", \"derived_facts\": [{\"derived_fact_id\": 1, \"fact\": \"…\"}]}, \"relevant_corpus_documents\": [{\"corpus_document_id\": 1, \"evidence\": [{\"text\": \"…\", \"fidelity\": \"verbatim\"}]}]}] — question is the retrieval query as asked, nothing else here may be handed to it. behavior is \"answer\", \"abstain\" (nothing in the corpus answers it — empty relevant_corpus_documents) or \"correct_premise\" (a false premise, corrected from the corpus); only abstain scores as unanswerable. text is the reference two of the four judged metrics compare against; derived_facts are the atomic claims a correct answer must contain, reported but non-voting. evidence.fidelity is \"verbatim\" (copied character for character — checked against the document, and the only kind lexical quote recall may score), \"paraphrase\" (the document's own claim, restated) or \"computed\" (worked out from a label, stated nowhere in the text).\n\nThe rule that earns its cost: every verbatim evidence entry must appear character for character in the document it cites. Quote recall, the Inspector's highlighted spans and the offline context metrics are all computed against those strings, so a ground truth that misquotes its corpus does not score worse — it scores confidently about text it never contained. Every bundled pair in fixtures/corpus_groundtruth_datasets/ meets it and is the working reference; schema_corpus.json and schema_groundtruth.json are the templates, not files to import.","index.chunker":"How one session is cut into the pieces that get embedded. \"fixed\" packs words up to a character budget; \"fixed-overlap\" slides a window so a sentence on a boundary appears in both neighbours; \"message\" keeps one message per piece; \"turn-pair\" keeps a question with its answer; \"session\" stores the whole session as one piece. \"semantic-drift\" cuts where consecutive messages stop resembling each other — the bottom third of that session's own similarity distribution, so no absolute threshold is assumed — and also at a size ceiling and at a short list of topic-change phrases, which is the only part of it written for one language.","index.chunk_chars":"How large a piece may be, in characters. Only the three length-based splitters read it; the rest cut on structure and grey it out. \"fixed\" and \"fixed-overlap\" treat it as the size of one piece, so it behaves as a target; \"semantic-drift\" treats it as a maximum, cutting a segment once adding the next message would take it past twice this value. Small pieces retrieve precisely and often lose the sentence that answers the question; large ones keep it and dilute precision.","index.overlap":"Characters repeated between neighbouring pieces, so a sentence sitting on a boundary is not cut in half. Only \"fixed-overlap\" slides a window, so only it reads this; an overlap at or above the piece size is halved rather than looping forever.","index.contextual":"Prepend a one-line header — date, mood, topics, threads — to every chunk before embedding it (Anthropic call this contextual retrieval). A chunk that says \"it got better\" is unsearchable without knowing what \"it\" was. Built from the corpus's own metadata, so it costs no model call and no summary; a field the corpus does not declare shows as an em dash, and the header is written in the corpus's declared language.","index.embedder":"Turns text into the vector the index is searched by, and the one choice that decides whether anything else matters. \"ascii-hash\" tokenises Latin letters and digits only, so a non-Latin corpus embeds to the zero vector — it is the baseline that makes the point, not a working option. \"token-hash\" and \"char-hash\" see any script, but only as letters, never as meaning. Two options load a real model and differ in what that costs: \"fastembed\" runs its own short ONNX list locally, \"sentence-transformers\" runs any HuggingFace checkpoint, which is the only way to reach an encoder tuned for one language — a Farsi-tuned one, for the bundled diary. It needs the local-embeddings extra and downloads weights. Whichever you pick, the model below decides which languages are covered.","index.embed_model":"Which real embedding model the chosen backend loads, and where a non-English corpus is won or lost: the best-known encoders (bge-small-en, all-MiniLM-L6) are English-only and will return confident numbers that measure nothing on text they cannot read, and the bundled corpus is Farsi — so every entry states its coverage and the multilingual ones are listed by name. Bigger is not automatically better — e5 is trained for retrieval while the paraphrase models are trained for similarity — and the E5 family needs its \"query:\"/\"passage:\" prefixes, which the lab applies for you. Changing this rebuilds the index, because it changes what is stored.","index.hierarchy":"Groups the chunks and indexes one summary per group *beside* them — the leaves always stay, because a summary that drops a detail makes the question about that detail unanswerable forever. Three families: \"louvain\", \"leiden\" and \"label-prop\" partition a graph built over the chunks; \"raptor\", \"agglomerative\" and \"kmeans\" cluster the chunk vectors; \"metadata\" groups by whatever storylines the corpus itself declares and is the control. **These are not GraphRAG.** GraphRAG extracts entities with a model; this lab builds offline, so the nodes are chunks and \"bipartite-terms\" below is the closest honest analogue. Expect leiden and louvain to tie on a small corpus: leiden's advantage is over badly-connected communities, which needs scale to show.","index.graph_source":"What an edge between two chunks means. \"knn\" is cosine similarity — each chunk joined to its nearest neighbours; \"lexical\" is shared rare words, which is what catches names and numbers a vector blurs; \"hybrid\" is both. \"bipartite-terms\" makes the rare words nodes too and partitions chunks and terms together, so a community has a nameable subject — the nearest thing to an entity graph without a model. A corpus's declared topics are deliberately not an edge source: that grouping is measurable on its own as hierarchy=\"metadata\", and mixing it in here would re-derive an already answered question inside a new one.","index.graph_knn":"How many nearest neighbours each chunk is joined to. Low values leave the graph in disconnected pieces and every piece becomes its own community; high values connect everything to everything and modularity collapses toward one giant group. The build reports both, so this is a knob you can tune by reading the index statistics rather than by running an evaluation.","index.granularity":"How coarse the grouping is, and it means two different things because the two families take two different parameters. For the graph partitions it is the modularity resolution: above 1.0 gives more, smaller communities. For the clusterings it is the group count, taken as granularity × √(n/2) over the leaf chunks — the usual rule of thumb. Either way 1.0 means \"this family's own default\".","index.hierarchy_levels":"How many times to group the groups. Level 1 summarises chunks; level 2 summarises those summaries. More levels answer broader questions and cost precision, because a summary of summaries is two extractions away from anything the corpus actually says.","index.min_group":"The smallest group worth summarising. Below it the members are left as leaves and no summary row is written — a \"summary\" of two chunks is the two chunks with a header on top, and it competes against its own members in the search.","index.summarizer":"How a group becomes one piece of text, without a model — a build that called an LLM would take hours instead of seconds and would let the offline fake backend fill the index with confident invention. \"centroid\" concatenates the members nearest the group's centre; \"lead-idf\" takes the sentences covering the most rare words; \"mmr\" picks members for coverage without repetition; \"card\" writes no prose at all — top terms, date span, member count, session ids — which is the cheapest and the most likely to help a counting question, because it states a number instead of asking the model to count chunks.","retrieval.summary_scope":"What the search is allowed to see. \"mixed\" puts summaries and leaves in one pool, so a hierarchy changes nothing until you move this; \"leaves\" ignores the summaries entirely and is the control that says whether building them bought anything; \"summaries\" searches only them. \"drill-down\" retrieves among summaries and then expands each to its members — the shape of GraphRAG's local search, and the answer to a failure measured here: a summary can be correct and reachable and still almost never retrieved, because twenty times more leaves outvote it.","retrieval.summary_boost":"Multiplies every summary's score before the candidates are cut. 1.0 is off. Applied before the cut and never after, because a summary that had not already survived the cut cannot be promoted into it — that version was measured and was a no-op that looked like a knob. Be careful with it even so: a boost lifts every summary equally, so it buys visibility for whichever kind of group is most numerous. \"drill-down\" is the targeted alternative.","retrieval.summary_levels":"Which levels of the hierarchy may be retrieved, as a space-separated list (\"1\", \"1 2\"). Empty means all of them. Worth setting when a deep hierarchy is answering broad questions well and specific ones badly: the top level is the one most likely to be retrieved for everything.","retrieval.retriever":"\"dense\" searches vectors (meaning), \"bm25\" searches words (exact names, numbers, rare terms), \"hybrid-rrf\" runs both and fuses the two rankings with Reciprocal Rank Fusion. Hybrid usually wins on a corpus full of proper nouns, which a vector blurs and a word match does not.","retrieval.k":"How many chunks the answerer finally sees. Raising it finds more evidence and lowers precision; it is the single knob that moves recall and precision in opposite directions.","retrieval.candidates":"How deep each retriever looks before fusion and reranking. Cheap to raise — nothing reads these yet — and it is what gives the reranker something to find.","retrieval.rrf_k":"The constant in Reciprocal Rank Fusion (1/(k+rank)). Higher flattens the ranking, so agreement between the two retrievers matters more than either one being confident.","retrieval.time_filter":"Dataset-specific: written for the bundled Farsi diary, not portable to a corpus in another language. It stays in the lab as the worked example of a general rule — a filter that knows its corpus's language and metadata beats a language-neutral pipeline. It reads Persian time language — «آذر», «تابستون», «سه ماه پیش» — as a Jalali date range and restricts the search to it, which is what stops a question about one month retrieving the whole year. A question in any other language matches nothing here, so the filter simply never fires and the search is unrestricted. Greyed out on a corpus with no label typed date-time (D5): with no dates on a chunk there is no span to restrict to, and the same absence leaves the recency reranker inert and a summary's date span blank.","retrieval.multi_query":"Dataset-specific: written for the bundled Farsi diary, not portable to a corpus in another language. It stays in the lab as the worked example of a general rule — a filter that knows its corpus's language and metadata beats a language-neutral pipeline. It searches several rule-based rewrites of the question — the question itself, a keyword-only form with the interrogatives stripped, and a synonym variant — and merges the hits. It costs nothing, and on the bundled diary moved quote recall from 0.489 to 0.512 with no model call. Both word lists are Persian and hand-written, so on another corpus every rewrite collapses back to the original question and this is a no-op.","retrieval.hyde":"Dataset-specific: written for the bundled Farsi diary, not portable to a corpus in another language. It stays in the lab as the worked example of a general rule — a filter that knows its corpus's language and metadata beats a language-neutral pipeline. It writes a hypothetical answer with a model and searches with that instead of the question, on the theory that an answer looks more like the text you are hunting for than a question does. Costs one LLM call per query. The prompt asks in Persian for a diary-style paragraph, so on another corpus it does not go quiet — it searches with text in the wrong language and register, which is worse than leaving it off.","retrieval.mmr_lambda":"Maximal Marginal Relevance. At 1.0 the top k are simply the best-scoring chunks, which often means several chunks from one session. Lower it to trade some relevance for spread across sessions.","retrieval.reranker":"Re-scores the candidates before the cut to k. \"lexical\" is free IDF coverage; \"recency\" prefers recent entries; \"agentic\" is the Generative Agents mix of relevance + recency + importance; \"cross-encoder\" reads question and chunk together with a real model; \"llm\" asks a model to score each one.","retrieval.rerank_depth":"How many candidates the reranker actually reads. The reranker is the expensive stage, so this is the cost dial: depth 20 with k 8 means twenty chunks scored to choose eight.","retrieval.recency_half_life_days":"How fast the recency reranker forgets. At 180 days an entry from six months ago counts half as much as today — right for \"how am I doing lately\", wrong for \"what happened last summer\". Greyed out on a corpus that declares no date-time label (D5): with no date on a chunk there is nothing to weigh by age.","retrieval.agentic_weights":"The three weights of the agentic reranker: relevance, recency, importance. Importance is the corpus's own numeric label declaring ranks: true (D6), rescaled to 0–1 by its declared minimum/maximum — a rating, a severity, whatever that corpus chose to rank chunks by. A corpus that declares no ranks label gives every chunk 0.0 importance, so that third weight shifts all scores equally and changes no ranking; the knob greys out for the same reason.","retrieval.grader":"The gate that makes abstention possible: chunks scoring below the threshold are dropped, and if nothing survives the pipeline refuses instead of answering from noise. \"none\" means every question gets an answer, including the ones the corpus never mentions.","retrieval.grade_threshold":"The score a chunk must clear to survive the gate. Measured on the bundled diary: the lexical gate had no usable setting (0.6 caught 6 of 8 unanswerable questions but wrongly refused 52% of the answerable ones), while an LLM gate at 0.4 refused 5 of 5 with 3% false refusals.","retrieval.max_context_chars":"Budget for the assembled context. When it is exceeded whole chunks are dropped, never truncated — half an entry reads as a complete one and invites an answer from a sentence whose second half changed the meaning.","generation.answerer":"\"none\" measures retrieval alone. \"extractive\" quotes the longest sentence from each of the top three chunks, tagged with its session — deterministic, free, and honest about quoting rather than answering. \"llm\" actually writes the answer.","generation.fact_judge":"Scores each answer against the ground truth's atomic derived_facts with a model. It is the only way to score facts when the answer and the reference are not in the same language — the bundled diary answers in Farsi against English facts, which no lexical metric can compare — and it is the metric that exposed generation as that corpus's bottleneck (coverage 0.261 against faithfulness 0.743).","run.mode":"Where the LLM stages run. \"Local (Ollama)\" is the lab default — free and private — and resets every stage to the lab's own defaults. \"OpenRouter\" switches the backend and presets the full pipeline onto gpt-5-nano (HyDE, LLM reranker, relevance gate, answerer and both judges); the relevance gate prefers a purpose-built reranker when OpenRouter's model list verifies one. No mode touches the index, so the embedder is left exactly where you set it. Picking a mode overwrites those stage choices; every knob can still be changed afterwards.","run.openrouter_key":"The key the OpenRouter backend calls with, entered here instead of in .env so a lab already running can reach a remote model. It is held in the lab process and written nowhere — not to a run file, not to the experiment ledger, not to your browser — so it is forgotten when the lab stops; OPENROUTER_API_KEY in the environment is still how a lab starts with one. Setting it does not change which backend runs: that is the dropdown above, and a model on this machine needs no key at all.","run.ragas_mode":"\"offline\" scores the retrieved context against the ground-truth quotes with string similarity — no model, no key, no variance. \"judged\" adds the five model-graded metrics: faithfulness, answer relevancy, factual correctness, and judged context precision and recall. Four of those five are what a configuration is actually chosen by. \"off\" skips RAGAS.","run.ragas_limit":"How many questions RAGAS scores, when judged metrics make the full set too slow or too expensive.","run.limit":"How many ground-truth questions to score. The subset is never the first n — it is drawn by the sampling below, striding across the set or taking an equal share of a labelled band — so a limit of 10 still covers the whole set instead of ten of one kind.","run.labels":"Restrict the run to questions whose declared labels match. One switch-group per label the loaded ground truth declares with a closed set of values or a glossary — read from that dataset, so the choices differ from one corpus to the next rather than naming a fixed list every corpus must share.","run.balance":"How a limited run chooses its questions. Naming a question label takes an equal share of that label's own values — \"difficulty\" takes an equal share of easy, medium and hard on a corpus that declares it; \"\" (stride) spreads across the set as it is, which means a corpus's most common band dominates the sample — on the bundled diary that is medium, 57 of 112, about half. It matters because the four deciding metrics are means over questions, so a skewed sample measures one band and reports it as the pipeline. The setting is recorded on every row rather than assumed, because it has not always been the same.","run.workers":"How many questions are scored in parallel. Only worth raising when a stage calls a model, where wall-clock is dominated by waiting.","run.label":"What this run is called in the leaderboard. Worth writing: a row named \"semantic-drift\" tells you nothing three days later.","model.expand":"Writes a hypothetical answer and searches with that instead of the question, on the theory that an answer looks more like the text you are hunting for than a question does. Only HyDE uses a model; multi-query expansion is rule-based and free. Dataset-specific: written for the bundled Farsi diary, not portable to a corpus in another language. It stays in the lab as the worked example of a general rule — a filter that knows its corpus's language and metadata beats a language-neutral pipeline. The prompt asks in Persian for a diary-style paragraph, so elsewhere it searches with text in the wrong language rather than going quiet.","model.rerank":"Reads the top candidates and scores each one against the question. It sits in the latency path of every query, so a slow model here is felt on every single question.","model.grade":"Decides whether a chunk is relevant at all. This is what lets the lab abstain rather than answer from noise: measured on the bundled diary, an LLM gate at 0.4 refused all five unanswerable questions while wrongly refusing 3% of the answerable ones — the lexical gate had no threshold that could do both.","model.answer":"Writes the answer from the retrieved context, cites session ids, and is the stage that must refuse when the corpus is silent. On the bundled diary generation is the bottleneck — faithfulness 0.743 against fact coverage 0.261 — so this is the interesting dropdown.","model.judge":"Checks an answer against the ground truth's atomic derived_facts. When the two are not in the same language — the bundled diary answers in Farsi against English facts — it is translating as well as judging, which is why no deterministic metric can replace it, and why a weak model here produces confidently wrong scores.","model.ragas":"The model RAGAS uses for faithfulness, answer relevancy and factual correctness. Separate from the answerer on purpose: a model grading its own output is not evidence.","metric.headline":"One comparable number for the leaderboard, weighted in the order that matters: did retrieval find the evidence, did the answering sentence survive chunking, was the evidence ranked first, and was an unanswerable question refused. Generation quality is deliberately excluded, so a config measured with the extractive answerer stays comparable to one measured with an LLM. Formula: 0.4·recall + 0.3·quote_recall + 0.2·ndcg + 0.1·abstained_correctly, renormalised over whichever of the four this run produced. Computed by metrics._headline — pure Python, no model, so it never varies between runs.","metric.recall":"Of the sessions the ground truth marks as evidence, the share that appear in the top k retrieved. This is the ceiling on everything downstream: an answer cannot cite what retrieval never returned. Questions with no evidence (the unanswerable ones) are excluded rather than scored zero. Formula: |gold ∩ top-k| / |gold|. Computed by metrics.recall_at_k — pure Python, no model, so it never varies between runs.","metric.quote_recall":"Session recall says the right session was found; this says the sentence that actually answers the question is inside the retrieved text. A splitter that cuts mid-thought scores well on the first and badly on this one, which is exactly the failure a session-level metric hides. Formula: matched quotes / total quotes, where a quote counts as matched if it is a substring of the normalised context or its longest common run covers >= 0.9 of it. Computed by metrics.quote_recall (difflib.SequenceMatcher) — pure Python, no model, so it never varies between runs.","metric.ndcg":"Rewards putting evidence at the top rather than merely including it somewhere in k. It matters because the answerer sees a context that gets truncated, so rank 1 and rank 8 are not worth the same. Formula: DCG/IDCG with binary gains: DCG = Σ gain_i / log2(i+2), IDCG the same sum over a perfect ordering. Computed by metrics.ndcg_at_k (math.log2) — pure Python, no model, so it never varies between runs.","metric.mrr":"How deep you have to read before the first correct hit. Sensitive to the top of the list only — useful next to nDCG, not instead of it. Formula: 1 / rank of the first evidence session, 0 if none was retrieved. Computed by metrics.mrr — pure Python, no model, so it never varies between runs.","metric.precision":"How much of what was retrieved is actually evidence. Low precision is not automatically bad — it is the cost of a wide k — but it is what dilutes the answerer's context and what MMR trades against. Formula: |top-k ∩ gold| / |top-k|. Computed by metrics.precision_at_k — pure Python, no model, so it never varies between runs.","metric.hit":"The most forgiving retrieval metric: did anything relevant come back at all. Useful for spotting total misses that an averaged recall softens. Formula: 1 if top-k contains any gold session, else 0. Computed by metrics.hit_at_k — pure Python, no model, so it never varies between runs.","metric.abstained_correctly":"The corpus genuinely has nothing about some of these questions. Saying so is the correct answer, and this is the fraction of those where the answerer refused instead of inventing something. Formula: refusals / unanswerable questions. Computed by metrics.score_question, reading the answerer's refusal flag — pure Python, no model, so it never varies between runs.","metric.false_abstention":"The other side of abstention, and the failure a badly tuned relevance gate produces: the evidence was there and the answerer still said it had nothing. Read it together with abstention — a gate that refuses everything scores perfectly on one and terribly here. Formula: refusals / answerable questions (lower is better). Computed by metrics.score_question, reading the answerer's refusal flag — pure Python, no model, so it never varies between runs.","metric.answer_similarity":"A blunt instrument, chosen because it is a *stable* one: no model, no variance. In practice it mostly tracks whether the same names, dates and numbers appear, and it punishes a correct short answer — which is why token F1 sits beside it. It compares characters, so it is meaningless when the answer and the reference are in different languages. Formula: difflib ratio = 2·M / T over normalised characters, where M is matched characters and T the combined length. Computed by metrics.answer_similarity (difflib.SequenceMatcher) — pure Python, no model, so it never varies between runs.","metric.answer_token_f1":"The SQuAD-style measure. It credits a short correct answer that a character-similarity ratio penalises purely for being short. Formula: F1 = 2PR/(P+R) over content words, P = overlap/|predicted|, R = overlap/|reference|, counting duplicates once. Computed by metrics.token_f1 (textnorm tokeniser) — pure Python, no model, so it never varies between runs.","metric.fact_coverage":"The ground truth breaks its reference answer into atomic derived_facts, and a judge checks each one against the produced answer. When the facts and the answer are in different languages — the bundled diary lists English facts against Farsi answers — the judge is translating as well as checking, which is why no deterministic metric replaces it, and why a weak model here produces confidently wrong scores. Formula: facts the judge marked present / derived_facts in the ground truth. Computed by evaluate.judge_derived_facts — an LLM judge (the \"Fact judge\" model role), so this number carries that model's variance.","metric.latency_ms":"Whole-pipeline wall clock, so it spans every stage rather than grading one. Dominated by whatever calls a model: an LLM reranker or gate moves this by orders of magnitude, a hash embedder does not. Formula: sum of the per-stage timings for one question, in milliseconds. Computed by time.perf_counter around each pipeline stage — pure Python, no model, so it never varies between runs.","metric.n_contexts":"What k actually delivered. It falls below k when the relevance gate removes chunks, which is the honest reason an answer had less to work with. Formula: mean number of contexts surviving rerank, gate and expansion. Computed by metrics.score_question — pure Python, no model, so it never varies between runs.","metric.context_chars":"How much text the answerer was given. Worth watching next to precision: two configs with the same recall can differ by 3x here, and the bigger one is paying for it in tokens and in dilution. Formula: mean total characters of the assembled context. Computed by metrics.score_question — pure Python, no model, so it never varies between runs.","metric.n_summaries":"How often the summary hierarchy was actually retrieved. This is the number that had to exist: a hierarchy scoring flat because nothing retrieved it and a hierarchy scoring flat because it did not help are different findings, and no other field on the row tells them apart. Zero here means the second reading is unavailable — which is exactly what the lab found once, having deleted five summary layers that were correct, reachable, and almost never retrieved. Formula: mean count of contexts whose layer is summary. Computed by metrics.score_question — pure Python, no model, so it never varies between runs.","metric.n_expanded":"Contexts that arrived because a summary was retrieved first and then expanded to the chunks it stands for — the drill-down scope. Zero under any other scope, by construction. Read beside Summaries used: the summary is what states an aggregate, and these are the leaves the answerer can actually quote. Formula: mean count of contexts expanded from a retrieved summary. Computed by metrics.score_question — pure Python, no model, so it never varies between runs.","metric.ragas_decision":"Four judged RAGAS metrics, averaged unweighted, and the only score that picks between configurations here. They were chosen because between them they cover both halves of the pipeline in both directions: context precision asks whether what came back was relevant and well ranked, context recall whether everything the answer needed was retrieved, faithfulness whether the answer stayed inside what was retrieved, and answer relevancy whether it addressed the question at all. Everything else on this screen is reported and none of it votes — factual correctness grades the fixture's phrasing as much as the pipeline, the offline context pair punish large chunks for being large, and our own deterministic metrics grade retrieval almost exclusively, so ranking on them rewards a config that finds the evidence and then says nothing useful about it. The cost of that choice is honest: all four come from a judge, so this number carries a model's variance, and it is only comparable between runs scored by the same RAGAS judge model. Formula: mean(faithfulness, answer_relevancy, llm_context_precision_with_reference, context_recall) — undefined unless all four are present. Computed by ragas_eval.decision_score over ragas 0.4.x metrics, scored by the RAGAS judge model — a model's verdict, so it varies.","metric.non_llm_context_recall":"RAGAS's context recall with the judge removed: it compares the retrieved context to the ground truth's verbatim evidence quotes as strings. Deterministic, and the RAGAS number to compare configs on — but it compares *whole strings*, so longer chunks score lower even when the answer is in them. Use quote recall to compare across chunkers. Formula: reference quotes matched in the retrieved contexts / reference quotes, matching by string similarity. Computed by ragas 0.4.x NonLLMContextRecall — string distance via rapidfuzz, no model involved.","metric.non_llm_context_precision_with_reference":"How much of the retrieved context matches the reference evidence, weighted towards the top of the ranking. Same whole-string caveat as its recall twin. Formula: mean precision@k over the retrieved contexts, a context counting as relevant when it is similar enough to a reference quote. Computed by ragas 0.4.x NonLLMContextPrecisionWithReference — string distance via rapidfuzz, no model involved.","metric.faithfulness":"RAGAS's own definition: the judge breaks the generated answer into individual claims, then checks each one against the retrieved context. An answer is faithful when the context supports every claim in it — so this measures invention, not correctness. A perfectly faithful answer can still be wrong if retrieval returned the wrong passage. Formula: supported claims / total claims in the answer. Computed by ragas 0.4.x Faithfulness, scored by the RAGAS judge model — a model's verdict, so it varies.","metric.answer_relevancy":"RAGAS asks the judge to invent the questions the answer would be a reply to, embeds them, and compares them with the real question. It catches answers that are true and on-topic but do not actually address what was asked. The vectors come from the embedder under test, so no external embedding API is called. Formula: mean cosine similarity between the original question and n questions the judge reverse-engineers from the answer. Computed by ragas 0.4.x ResponseRelevancy (judge + the lab's own embedder for the vectors), scored by the RAGAS judge model — a model's verdict, so it varies.","metric.factual_correctness(mode=f1)":"Unlike faithfulness, this compares the answer to the *reference answer* rather than to the retrieved context — so it measures being right, not merely being grounded. Both directions are counted, which is why it is an F1: an answer that omits half the reference loses recall, one that adds unsupported detail loses precision. Formula: F1 = 2PR/(P+R) over claims: P = claims in the answer supported by the reference, R = claims in the reference present in the answer. Computed by ragas 0.4.x FactualCorrectness(mode=f1), scored by the RAGAS judge model — a model's verdict, so it varies.","metric.llm_context_precision_with_reference":"The judged twin of the offline precision metric. It is immune to the whole-string penalty that makes the offline pair unfair to large chunks, at the cost of a model call per context and a model's variance. Formula: mean precision@k where the judge, not a string metric, decides whether each retrieved context is relevant. Computed by ragas 0.4.x LLMContextPrecisionWithReference, scored by the RAGAS judge model — a model's verdict, so it varies.","metric.context_recall":"The judge splits the reference answer into claims and asks, for each one, whether the retrieved context could support it. It answers \"was everything needed retrieved?\" without depending on the exact wording of a quote. Formula: reference claims attributable to the retrieved context / reference claims. Computed by ragas 0.4.x LLMContextRecall, scored by the RAGAS judge model — a model's verdict, so it varies."},"corpus":{"documents":167,"parts":998,"questions":112,"query_date":"2026-07-28"},"capabilities":{"fastembed":true,"sentence_transformers":true,"cross_encoder":true,"cross_encoder_model":"jinaai/jina-reranker-v2-base-multilingual","fastembed_model":"sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2","llm":true,"llm_provider":"ollama","llm_model":"4skl/gemma4-e2b-mtp","ollama_base_url":"http://localhost:11434/v1","ragas":{"installed":true,"llm_ready":true,"version":"0.4.3","notes":[],"offline_metrics":["non_llm_context_precision_with_reference","non_llm_context_recall"],"llm_metrics":["faithfulness","answer_relevancy","factual_correctness(mode=f1)","llm_context_precision_with_reference","context_recall"],"install_hint":"uv sync  (these are locked dependencies now: ragas==0.4.*, langchain-community>=0.3.31,<0.4, rapidfuzz)"},"openrouter_key":{"set":true,"source":"environment","hint":"sk-or…8ffc"},"storage":{"index":"memory","runs":".runs","experiments":"/private/tmp/claude-501/-Users-mohakashani-Projects-raglab/eb8c0724-24c1-4b39-aa25-0aab04fae211/scratchpad/smoke-dbs/raglab.db"}},"datasets":[{"id":"diary-fa","name":"Diary Year Farsi Corpus","description":"A synthetic one-year Farsi diary: conversations between a diarist and an assistant, one document per session.","language":"fa","source":"bundled","path":"fixtures/corpus_groundtruth_datasets/diary_year_fa_corpus.json","documents":167,"parts":998,"questions":112,"query_date":"2026-07-28T00:00:00Z","period":{"from":"2025-08-02T23:10:00Z","to":"2026-07-27T21:45:00Z"},"labels":["feeling","feeling_confidence","input_mode","recorded_at","recurring_threads","role","topics","topics_confidence"],"id_corpora":0,"label_declarations":[{"name":"feeling","type":"string","levels":[],"extracted":true,"confidence_for":""},{"name":"feeling_confidence","type":"number","levels":[],"extracted":false,"confidence_for":"feeling"},{"name":"input_mode","type":"string","levels":["text","voice"],"extracted":false,"confidence_for":""},{"name":"recorded_at","type":"string","levels":[],"extracted":false,"confidence_for":""},{"name":"recurring_threads","type":"array","levels":["jacqueline-fights","job-search","turing-college","failed-ambitions","apartment","money-investing","tax-problem","neighbor-noise","mother-health","articles-research","work-stress","side-project","sleep-health","travel-dream","family-gatherings","car-trouble","good-days","habit-tracking"],"extracted":false,"confidence_for":""},{"name":"role","type":"string","levels":["user","assistant"],"extracted":false,"confidence_for":""},{"name":"topics","type":"array","levels":[],"extracted":true,"confidence_for":""},{"name":"topics_confidence","type":"object","levels":[],"extracted":false,"confidence_for":"topics"}],"question_label_declarations":[{"name":"difficulty","type":"string","levels":["easy","medium","hard"],"extracted":false,"confidence_for":""},{"name":"query_threads","type":"array","levels":["jacqueline-fights","job-search","turing-college","failed-ambitions","apartment","money-investing","tax-problem","neighbor-noise","mother-health","articles-research","work-stress","side-project","sleep-health","travel-dream","family-gatherings","car-trouble","good-days","habit-tracking"],"extracted":false,"confidence_for":""},{"name":"question_translation_en","type":"string","levels":[],"extracted":false,"confidence_for":""},{"name":"question_type","type":"string","levels":["single-hop","temporal","multi-hop","aggregation","knowledge-update","commitment","entity","pattern","abstention","adversarial","habit"],"extracted":false,"confidence_for":""},{"name":"resolved_time_scope","type":"object","levels":[],"extracted":false,"confidence_for":""}],"date_label":"recorded_at","ranks_label":"","question_labels":{"question_type":["single-hop","temporal","multi-hop","aggregation","knowledge-update","commitment","entity","pattern","abstention","adversarial","habit"],"difficulty":["easy","medium","hard"],"query_threads":["jacqueline-fights","job-search","turing-college","failed-ambitions","apartment","money-investing","tax-problem","neighbor-noise","mother-health","articles-research","work-stress","side-project","sleep-health","travel-dream","family-gatherings","car-trouble","good-days","habit-tracking"]},"balances":["","difficulty","query_threads","question_type"]},{"id":"meetings-de","name":"Team meeting notes (German)","description":"Synthetic German weekly meeting notes for a fictional software team over six months. A third script family, compound nouns and dates written in words — where an English-only embedder separates sharply from a multilingual one. All names, companies and events are fictional.","language":"de","source":"bundled","path":"fixtures/corpus_groundtruth_datasets/meetings_de_corpus.json","documents":15,"parts":30,"questions":12,"query_date":"2026-07-01T00:00:00Z","period":{"from":"2026-01-13T00:00:00Z","to":"2026-06-30T00:00:00Z"},"labels":["meeting_date","mood_arousal","mood_label","mood_valence","role","threads","topics"],"id_corpora":0,"label_declarations":[{"name":"meeting_date","type":"string","levels":[],"extracted":false,"confidence_for":""},{"name":"mood_arousal","type":"number","levels":[],"extracted":false,"confidence_for":""},{"name":"mood_label","type":"string","levels":[],"extracted":false,"confidence_for":""},{"name":"mood_valence","type":"number","levels":[],"extracted":false,"confidence_for":""},{"name":"role","type":"string","levels":["user","assistant"],"extracted":false,"confidence_for":""},{"name":"threads","type":"array","levels":[],"extracted":false,"confidence_for":""},{"name":"topics","type":"array","levels":[],"extracted":false,"confidence_for":""}],"question_label_declarations":[{"name":"difficulty","type":"string","levels":["easy","medium","hard"],"extracted":false,"confidence_for":""},{"name":"question_translation_en","type":"string","levels":[],"extracted":false,"confidence_for":""},{"name":"question_type","type":"string","levels":["single-hop","temporal","knowledge-update","commitment","entity","multi-hop","aggregation","abstention","adversarial"],"extracted":false,"confidence_for":""},{"name":"time_scope","type":"object","levels":[],"extracted":false,"confidence_for":""}],"date_label":"meeting_date","ranks_label":"mood_valence","question_labels":{"question_type":["single-hop","temporal","knowledge-update","commitment","entity","multi-hop","aggregation","abstention","adversarial"],"difficulty":["easy","medium","hard"]},"balances":["","difficulty","question_type"]},{"id":"research-multihop","name":"Research notes — multi-hop","description":"Synthetic English reading notes following one replication dispute across eight months. Deliberately weighted to multi-hop and aggregation: most answers need two or three sessions, so a pipeline that retrieves one perfect chunk still scores badly. All authors, institutions and findings are fictional.","language":"en","source":"bundled","path":"fixtures/corpus_groundtruth_datasets/research_multihop_corpus.json","documents":18,"parts":36,"questions":14,"query_date":"2026-05-01T00:00:00Z","period":{"from":"2025-09-02T00:00:00Z","to":"2026-04-21T00:00:00Z"},"labels":["recorded_at","role","threads","topics"],"id_corpora":0,"label_declarations":[{"name":"recorded_at","type":"string","levels":[],"extracted":false,"confidence_for":""},{"name":"role","type":"string","levels":["user","assistant"],"extracted":false,"confidence_for":""},{"name":"threads","type":"array","levels":[],"extracted":false,"confidence_for":""},{"name":"topics","type":"array","levels":[],"extracted":false,"confidence_for":""}],"question_label_declarations":[{"name":"difficulty","type":"string","levels":["easy","medium","hard"],"extracted":false,"confidence_for":""},{"name":"question_type","type":"string","levels":["single-hop","multi-hop","aggregation","temporal","entity","pattern","abstention","adversarial"],"extracted":false,"confidence_for":""}],"date_label":"recorded_at","ranks_label":"","question_labels":{"question_type":["single-hop","multi-hop","aggregation","temporal","entity","pattern","abstention","adversarial"],"difficulty":["easy","medium","hard"]},"balances":["","difficulty","question_type"]},{"id":"smoke-mini","name":"Smoke set — five sessions","description":"Five sessions and six questions across all three difficulty bands. Too small to measure anything and not meant to: it is the set you run end to end in seconds to check that a pipeline works before spending an hour on one that does not. Deliberately declares no date-typed label and no ranks label, so this is the corpus that exercises the \"no time behaviour\" path on every run.","language":"en","source":"bundled","path":"fixtures/corpus_groundtruth_datasets/smoke_mini_corpus.json","documents":5,"parts":10,"questions":6,"query_date":"2026-06-08T00:00:00Z","period":{},"labels":["role","topics"],"id_corpora":0,"label_declarations":[{"name":"role","type":"string","levels":["user","assistant"],"extracted":false,"confidence_for":""},{"name":"topics","type":"array","levels":[],"extracted":false,"confidence_for":""}],"question_label_declarations":[{"name":"difficulty","type":"string","levels":["easy","medium","hard"],"extracted":false,"confidence_for":""},{"name":"question_type","type":"string","levels":["single-hop","temporal","multi-hop","abstention"],"extracted":false,"confidence_for":""}],"date_label":"","ranks_label":"","question_labels":{"question_type":["single-hop","temporal","multi-hop","abstention"],"difficulty":["easy","medium","hard"]},"balances":["","difficulty","question_type"]},{"id":"support-en","name":"Product support — one account, one year","description":"Synthetic English support conversations between one customer and the support team of a fictional analytics product, over a year. A Latin-script corpus with short factual turns, dates and identifiers — the mirror of the Farsi diary, and the corpus where an ASCII embedder is genuinely competitive rather than scoring at chance. All names, products and events are fictional.","language":"en","source":"bundled","path":"fixtures/corpus_groundtruth_datasets/support_en_corpus.json","documents":20,"parts":40,"questions":15,"query_date":"2026-07-29T00:00:00Z","period":{"from":"2025-11-04T00:00:00Z","to":"2026-07-28T00:00:00Z"},"labels":["channel","mood","recorded_at","recurring_threads","role","topics"],"id_corpora":0,"label_declarations":[{"name":"channel","type":"string","levels":["chat","email"],"extracted":false,"confidence_for":""},{"name":"mood","type":"string","levels":[],"extracted":false,"confidence_for":""},{"name":"recorded_at","type":"string","levels":[],"extracted":false,"confidence_for":""},{"name":"recurring_threads","type":"array","levels":[],"extracted":false,"confidence_for":""},{"name":"role","type":"string","levels":["user","assistant"],"extracted":false,"confidence_for":""},{"name":"topics","type":"array","levels":[],"extracted":false,"confidence_for":""}],"question_label_declarations":[{"name":"difficulty","type":"string","levels":[],"extracted":false,"confidence_for":""},{"name":"question_type","type":"string","levels":[],"extracted":false,"confidence_for":""}],"date_label":"recorded_at","ranks_label":"","question_labels":{},"balances":[""]},{"id":"smoke-import-check","name":"Smoke set — five sessions","description":"Five sessions and six questions across all three difficulty bands. Too small to measure anything and not meant to: it is the set you run end to end in seconds to check that a pipeline works before spending an hour on one that does not. Deliberately declares no date-typed label and no ranks label, so this is the corpus that exercises the \"no time behaviour\" path on every run.","language":"en","source":"imported","path":".datasets/smoke-import-check_corpus.json","documents":5,"parts":10,"questions":6,"query_date":"2026-06-08T00:00:00Z","period":{},"labels":["role","topics"],"id_corpora":0,"label_declarations":[{"name":"role","type":"string","levels":["user","assistant"],"extracted":false,"confidence_for":""},{"name":"topics","type":"array","levels":[],"extracted":false,"confidence_for":""}],"question_label_declarations":[{"name":"difficulty","type":"string","levels":["easy","medium","hard"],"extracted":false,"confidence_for":""},{"name":"question_type","type":"string","levels":["single-hop","temporal","multi-hop","abstention"],"extracted":false,"confidence_for":""}],"date_label":"","ranks_label":"","question_labels":{"question_type":["single-hop","temporal","multi-hop","abstention"],"difficulty":["easy","medium","hard"]},"balances":["","difficulty","question_type"]}],"indexes":[]}