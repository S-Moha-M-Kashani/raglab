{"format": "raglab-experiment", "version": 1, "settings": {"config": {"index": {"dataset": "meetings-de", "chunker": "semantic-drift", "chunk_chars": 500, "overlap": 100, "contextual": true, "embedder": "sentence-transformers", "embed_model": "", "hierarchy": "metadata", "graph_source": "hybrid", "graph_knn": 8, "granularity": 1, "hierarchy_levels": 1, "min_group": 3, "summarizer": "centroid"}, "retrieval": {"retriever": "hybrid-rrf", "k": 4, "candidates": 20, "rrf_k": 60, "time_filter": true, "multi_query": true, "hyde": true, "expansion_model": "", "mmr_lambda": 1, "reranker": "llm", "rerank_depth": 8, "reranker_model": "", "recency_half_life_days": 180, "agentic_weights": [1, 0.3, 0.2], "grader": "llm", "grade_threshold": 0, "grader_model": "", "max_context_chars": 6000, "summary_scope": "mixed", "summary_boost": 1, "summary_levels": ""}, "generation": {"answerer": "llm", "model": "", "key_facts_judge": true, "judge_model": "", "ragas_model": ""}, "label": "full stack, one question, every stage on (codex)"}, "ui": {"mode": "codex", "ragas_mode": "llm", "limit": 1, "ragas_limit": 1, "types": []}}, "evaluation": {"execution": {"provider": "codex", "models": {"expand": "gpt-5.6-luna", "rerank": "gpt-5.6-luna", "grade": "gpt-5.6-luna", "answer": "gpt-5.6-luna", "judge": "gpt-5.6-luna", "ragas": "gpt-5.6-luna"}}, "metric_catalogue": [{"key": "headline", "label": "Composite", "short": "weighted retrieval score", "step": "", "formula": "0.4·recall + 0.3·quote_recall + 0.2·ndcg + 0.1·abstained_correctly, renormalised over whichever of the four this run produced", "library": "metrics._headline — pure Python, no model, so it never varies between runs", "help": "One comparable number for the leaderboard, weighted in the order that matters: did retrieval find the evidence, did the answering sentence survive chunking, was the evidence ranked first, and was an unanswerable question refused. Generation quality is deliberately excluded, so a config measured with the extractive answerer stays comparable to one measured with an LLM."}, {"key": "recall", "label": "Recall@k", "short": "evidence sessions found", "step": "retrieval", "formula": "|gold ∩ top-k| / |gold|", "library": "metrics.recall_at_k — pure Python, no model, so it never varies between runs", "help": "Of the sessions the ground truth marks as evidence, the share that appear in the top k retrieved. This is the ceiling on everything downstream: an answer cannot cite what retrieval never returned. Questions with no evidence (the unanswerable ones) are excluded rather than scored zero."}, {"key": "quote_recall", "label": "Quote recall", "short": "answering sentence survived chunking", "step": "retrieval", "formula": "matched quotes / total quotes, where a quote counts as matched if it is a substring of the normalised context or its longest common run covers >= 0.9 of it", "library": "metrics.quote_recall (difflib.SequenceMatcher) — pure Python, no model, so it never varies between runs", "help": "Session recall says the right session was found; this says the sentence that actually answers the question is inside the retrieved text. A splitter that cuts mid-thought scores well on the first and badly on this one, which is exactly the failure a session-level metric hides."}, {"key": "ndcg", "label": "nDCG@k", "short": "evidence ranked first", "step": "retrieval", "formula": "DCG/IDCG with binary gains: DCG = Σ gain_i / log2(i+2), IDCG the same sum over a perfect ordering", "library": "metrics.ndcg_at_k (math.log2) — pure Python, no model, so it never varies between runs", "help": "Rewards putting evidence at the top rather than merely including it somewhere in k. It matters because the answerer sees a context that gets truncated, so rank 1 and rank 8 are not worth the same."}, {"key": "mrr", "label": "MRR", "short": "rank of first evidence", "step": "retrieval", "formula": "1 / rank of the first evidence session, 0 if none was retrieved", "library": "metrics.mrr — pure Python, no model, so it never varies between runs", "help": "How deep you have to read before the first correct hit. Sensitive to the top of the list only — useful next to nDCG, not instead of it."}, {"key": "precision", "label": "Precision@k", "short": "signal to noise", "step": "retrieval", "formula": "|top-k ∩ gold| / |top-k|", "library": "metrics.precision_at_k — pure Python, no model, so it never varies between runs", "help": "How much of what was retrieved is actually evidence. Low precision is not automatically bad — it is the cost of a wide k — but it is what dilutes the answerer's context and what MMR trades against."}, {"key": "hit", "label": "Hit@k", "short": "at least one evidence session", "step": "retrieval", "formula": "1 if top-k contains any gold session, else 0", "library": "metrics.hit_at_k — pure Python, no model, so it never varies between runs", "help": "The most forgiving retrieval metric: did anything relevant come back at all. Useful for spotting total misses that an averaged recall softens."}, {"key": "latest_state_hit", "label": "Latest state", "short": "changed facts, current version", "step": "retrieval", "formula": "1 if the newest evidence session is in top-k, else 0 (knowledge-update questions only)", "library": "metrics.latest_state_session — pure Python, no model, so it never varies between runs", "help": "On a fact that changed over time, retrieving only the superseded state is worse than retrieving nothing: it produces a confident, stale answer. This checks that the most recent evidence session was found."}, {"key": "abstained_correctly", "label": "Abstention", "short": "unanswerable refused", "step": "generation", "formula": "refusals / unanswerable questions", "library": "metrics.score_question, reading the answerer's refusal flag — pure Python, no model, so it never varies between runs", "help": "The corpus genuinely has nothing about some of these questions. Saying so is the correct answer, and this is the fraction of those where the answerer refused instead of inventing something."}, {"key": "false_abstention", "label": "False refusals", "short": "answerable wrongly refused", "step": "generation", "formula": "refusals / answerable questions (lower is better)", "library": "metrics.score_question, reading the answerer's refusal flag — pure Python, no model, so it never varies between runs", "help": "The other side of abstention, and the failure a badly tuned relevance gate produces: the evidence was there and the answerer still said it had nothing. Read it together with abstention — a gate that refuses everything scores perfectly on one and terribly here."}, {"key": "answer_similarity", "label": "Answer similarity", "short": "vs the reference answer", "step": "generation", "formula": "difflib ratio = 2·M / T over normalised characters, where M is matched characters and T the combined length", "library": "metrics.answer_similarity (difflib.SequenceMatcher) — pure Python, no model, so it never varies between runs", "help": "A blunt instrument, chosen because it is a *stable* one: no model, no variance. In practice it mostly tracks whether the same names, dates and numbers appear, and it punishes a correct short answer — which is why token F1 sits beside it. It compares characters, so it is meaningless when the answer and the reference are in different languages."}, {"key": "answer_token_f1", "label": "Answer token F1", "short": "unigram overlap", "step": "generation", "formula": "F1 = 2PR/(P+R) over content words, P = overlap/|predicted|, R = overlap/|reference|, counting duplicates once", "library": "metrics.token_f1 (textnorm tokeniser) — pure Python, no model, so it never varies between runs", "help": "The SQuAD-style measure. It credits a short correct answer that a character-similarity ratio penalises purely for being short."}, {"key": "key_fact_coverage", "label": "Key facts", "short": "judged fact coverage", "step": "generation", "formula": "facts the judge marked present / key facts in the ground truth", "library": "evaluate.judge_key_facts — an LLM judge (the \"Key-facts judge\" model role), so this number carries that model's variance", "help": "The ground truth lists the atomic facts a correct answer must contain, and a judge checks each one. When the facts and the answers are in different languages — the bundled diary lists English facts against Farsi answers — the judge is translating as well as checking, which is why no deterministic metric replaces it, and why a weak model here produces confidently wrong scores."}, {"key": "latency_ms", "label": "Latency", "short": "ms per question", "step": "", "formula": "sum of the per-stage timings for one question, in milliseconds", "library": "time.perf_counter around each pipeline stage — pure Python, no model, so it never varies between runs", "help": "Whole-pipeline wall clock, so it spans every stage rather than grading one. Dominated by whatever calls a model: an LLM reranker or gate moves this by orders of magnitude, a hash embedder does not."}, {"key": "n_contexts", "label": "Contexts", "short": "chunks handed to the answerer", "step": "retrieval", "formula": "mean number of contexts surviving rerank, gate and expansion", "library": "metrics.score_question — pure Python, no model, so it never varies between runs", "help": "What k actually delivered. It falls below k when the relevance gate removes chunks, which is the honest reason an answer had less to work with."}, {"key": "context_chars", "label": "Context size", "short": "characters of context", "step": "retrieval", "formula": "mean total characters of the assembled context", "library": "metrics.score_question — pure Python, no model, so it never varies between runs", "help": "How much text the answerer was given. Worth watching next to precision: two configs with the same recall can differ by 3x here, and the bigger one is paying for it in tokens and in dilution."}, {"key": "n_summaries", "label": "Summaries used", "short": "summary rows in the context", "step": "index", "formula": "mean count of contexts whose layer is summary", "library": "metrics.score_question — pure Python, no model, so it never varies between runs", "help": "How often the summary hierarchy was actually retrieved. This is the number that had to exist: a hierarchy scoring flat because nothing retrieved it and a hierarchy scoring flat because it did not help are different findings, and no other field on the row tells them apart. Zero here means the second reading is unavailable — which is exactly what the lab found once, having deleted five summary layers that were correct, reachable, and almost never retrieved."}, {"key": "n_expanded", "label": "Drilled down", "short": "members reached through a summary", "step": "retrieval", "formula": "mean count of contexts expanded from a retrieved summary", "library": "metrics.score_question — pure Python, no model, so it never varies between runs", "help": "Contexts that arrived because a summary was retrieved first and then expanded to the chunks it stands for — the drill-down scope. Zero under any other scope, by construction. Read beside Summaries used: the summary is what states an aggregate, and these are the leaves the answerer can actually quote."}, {"key": "ragas_decision", "label": "RAGAS decision score", "short": "the number the architecture was chosen by", "step": "", "formula": "mean(faithfulness, answer_relevancy, llm_context_precision_with_reference, context_recall) — undefined unless all four are present", "library": "ragas_eval.decision_score over ragas 0.4.x metrics, scored by the RAGAS judge model — a model's verdict, so it varies", "help": "Four judged RAGAS metrics, averaged unweighted, and the only score that picks between configurations here. They were chosen because between them they cover both halves of the pipeline in both directions: context precision asks whether what came back was relevant and well ranked, context recall whether everything the answer needed was retrieved, faithfulness whether the answer stayed inside what was retrieved, and answer relevancy whether it addressed the question at all. Everything else on this screen is reported and none of it votes — factual correctness grades the fixture's phrasing as much as the pipeline, the offline context pair punish large chunks for being large, and our own deterministic metrics grade retrieval almost exclusively, so ranking on them rewards a config that finds the evidence and then says nothing useful about it. The cost of that choice is honest: all four come from a judge, so this number carries a model's variance, and it is only comparable between runs scored by the same RAGAS judge model."}, {"key": "non_llm_context_recall", "label": "Context recall (offline)", "short": "RAGAS, string distance", "step": "retrieval", "formula": "reference quotes matched in the retrieved contexts / reference quotes, matching by string similarity", "library": "ragas 0.4.x NonLLMContextRecall — string distance via rapidfuzz, no model involved", "help": "RAGAS's context recall with the judge removed: it compares the retrieved context to the ground truth's verbatim evidence quotes as strings. Deterministic, and the RAGAS number to compare configs on — but it compares *whole strings*, so longer chunks score lower even when the answer is in them. Use quote recall to compare across chunkers."}, {"key": "non_llm_context_precision_with_reference", "label": "Context precision (offline)", "short": "RAGAS, string distance", "step": "retrieval", "formula": "mean precision@k over the retrieved contexts, a context counting as relevant when it is similar enough to a reference quote", "library": "ragas 0.4.x NonLLMContextPrecisionWithReference — string distance via rapidfuzz, no model involved", "help": "How much of the retrieved context matches the reference evidence, weighted towards the top of the ranking. Same whole-string caveat as its recall twin."}, {"key": "faithfulness", "label": "Faithfulness", "short": "answer supported by the context", "step": "generation", "formula": "supported claims / total claims in the answer", "library": "ragas 0.4.x Faithfulness, scored by the RAGAS judge model — a model's verdict, so it varies", "help": "RAGAS's own definition: the judge breaks the generated answer into individual claims, then checks each one against the retrieved context. An answer is faithful when the context supports every claim in it — so this measures invention, not correctness. A perfectly faithful answer can still be wrong if retrieval returned the wrong passage."}, {"key": "answer_relevancy", "label": "Answer relevancy", "short": "does it answer the question", "step": "generation", "formula": "mean cosine similarity between the original question and n questions the judge reverse-engineers from the answer", "library": "ragas 0.4.x ResponseRelevancy (judge + the lab's own embedder for the vectors), scored by the RAGAS judge model — a model's verdict, so it varies", "help": "RAGAS asks the judge to invent the questions the answer would be a reply to, embeds them, and compares them with the real question. It catches answers that are true and on-topic but do not actually address what was asked. The vectors come from the embedder under test, so no external embedding API is called."}, {"key": "factual_correctness(mode=f1)", "label": "Factual correctness", "short": "claims vs the reference answer", "step": "generation", "formula": "F1 = 2PR/(P+R) over claims: P = claims in the answer supported by the reference, R = claims in the reference present in the answer", "library": "ragas 0.4.x FactualCorrectness(mode=f1), scored by the RAGAS judge model — a model's verdict, so it varies", "help": "Unlike faithfulness, this compares the answer to the *reference answer* rather than to the retrieved context — so it measures being right, not merely being grounded. Both directions are counted, which is why it is an F1: an answer that omits half the reference loses recall, one that adds unsupported detail loses precision."}, {"key": "llm_context_precision_with_reference", "label": "Context precision (judged)", "short": "RAGAS, judged relevance", "step": "retrieval", "formula": "mean precision@k where the judge, not a string metric, decides whether each retrieved context is relevant", "library": "ragas 0.4.x LLMContextPrecisionWithReference, scored by the RAGAS judge model — a model's verdict, so it varies", "help": "The judged twin of the offline precision metric. It is immune to the whole-string penalty that makes the offline pair unfair to large chunks, at the cost of a model call per context and a model's variance."}, {"key": "context_recall", "label": "Context recall (judged)", "short": "RAGAS, judged coverage", "step": "retrieval", "formula": "reference claims attributable to the retrieved context / reference claims", "library": "ragas 0.4.x LLMContextRecall, scored by the RAGAS judge model — a model's verdict, so it varies", "help": "The judge splits the reference answer into claims and asks, for each one, whether the retrieved context could support it. It answers \"was everything needed retrieved?\" without depending on the exact wording of a quote."}], "stage_results": {"retrieval": {"metrics": {"recall": 1.0, "precision": 0.25, "mrr": 1.0, "ndcg": 1.0, "hit": 1.0, "quote_recall": 1.0, "latest_state_hit": null, "n_contexts": 4.0, "n_expanded": 0.0, "context_chars": 995.0, "context_recall": 1.0, "llm_context_precision_with_reference": 1.0, "non_llm_context_precision_with_reference": 0.0, "non_llm_context_recall": 0.0}}, "generation": {"metrics": {"false_abstention": 0.0, "abstained_correctly": null, "answer_similarity": 0.3857, "answer_token_f1": 0.3077, "key_fact_coverage": 1.0, "answer_relevancy": 1.0, "factual_correctness(mode=f1)": 0.0, "faithfulness": 1.0}}, "overall": {"metrics": {"latency_ms": 25834.5, "headline": 1.0}}, "index": {"statistics": {"collection": "raglab-ac63eaccfe49", "chunks": 17, "avg_chars": 243.7, "p95_chars": 290, "embed_dim": 1024, "build_seconds": 6.45, "reused": false}, "metrics": {"n_summaries": 0.0}}}, "result": {"run_id": "20260822-230227-ac63ea", "label": "full stack, one question, every stage on (codex)", "config": {"index": {"dataset": "meetings-de", "chunker": "semantic-drift", "chunk_chars": 500, "overlap": 100, "contextual": true, "embedder": "sentence-transformers", "embed_model": "", "hierarchy": "metadata", "graph_source": "hybrid", "graph_knn": 8, "granularity": 1, "hierarchy_levels": 1, "min_group": 3, "summarizer": "centroid"}, "retrieval": {"retriever": "hybrid-rrf", "k": 4, "candidates": 20, "rrf_k": 60, "time_filter": true, "multi_query": true, "hyde": true, "expansion_model": "", "mmr_lambda": 1, "reranker": "llm", "rerank_depth": 8, "reranker_model": "", "recency_half_life_days": 180, "agentic_weights": [1, 0.3, 0.2], "grader": "llm", "grade_threshold": 0, "grader_model": "", "max_context_chars": 6000, "summary_scope": "mixed", "summary_boost": 1, "summary_levels": ""}, "generation": {"answerer": "llm", "model": "", "key_facts_judge": true, "judge_model": "", "ragas_model": ""}, "label": "full stack, one question, every stage on (codex)"}, "dataset": "meetings-de", "index": {"collection": "raglab-ac63eaccfe49", "chunks": 17, "avg_chars": 243.7, "p95_chars": 290, "embed_dim": 1024, "build_seconds": 6.45, "reused": false}, "summary": {"overall": {"recall": 1.0, "precision": 0.25, "mrr": 1.0, "ndcg": 1.0, "hit": 1.0, "quote_recall": 1.0, "latest_state_hit": null, "false_abstention": 0.0, "abstained_correctly": null, "answer_similarity": 0.3857, "answer_token_f1": 0.3077, "key_fact_coverage": 1.0, "latency_ms": 25834.5, "n_contexts": 4.0, "n_summaries": 0.0, "n_expanded": 0.0, "context_chars": 995.0, "headline": 1.0}, "by_type": {"single-hop": {"n": 1, "recall": 1.0, "quote_recall": 1.0, "ndcg": 1.0, "hit": 1.0, "abstained_correctly": null, "false_abstention": 0.0, "answer_similarity": 0.3857}}, "by_difficulty": {"easy": {"n": 1, "recall": 1.0}}, "n_questions": 1}, "rows": [{"id": "mtg-001", "type": "single-hop", "difficulty": "easy", "answerable": true, "retrieved_sessions": ["mtg-0113", "mtg-0630", "mtg-0120", "mtg-0616"], "n_contexts": 4, "n_summaries": 0, "n_expanded": 0, "context_chars": 995, "abstained": false, "time_scope": {"from": "2026-01-01", "to": "2026-12-31", "label": "2026", "kind": "gregorian-year"}, "latency_ms": 25834.5, "recall": 1.0, "precision": 0.25, "mrr": 1.0, "ndcg": 1.0, "hit": 1.0, "quote_recall": 1.0, "false_abstention": 0.0, "answer": "Das Budget für 2026 beträgt 480.000 Euro. [mtg-0113 | 2026-01-13]", "answer_similarity": 0.38571428571428573, "answer_token_f1": 0.3076923076923077, "key_fact_coverage": 1.0}], "ragas": {"mode": "llm", "metrics": {"answer_relevancy": 1.0, "context_recall": 1.0, "factual_correctness(mode=f1)": 0.0, "faithfulness": 1.0, "llm_context_precision_with_reference": 1.0, "non_llm_context_precision_with_reference": 0.0, "non_llm_context_recall": 0.0}, "n_samples": 1, "skipped": 0, "decision": 1.0, "decision_spread": {"n": 1, "mean": 1.0, "stderr": null}, "decision_metrics": ["faithfulness", "answer_relevancy", "llm_context_precision_with_reference", "context_recall"], "notes": ["judged by gpt-5.6-luna via codex"], "judge": {"provider": "codex", "model": "gpt-5.6-luna"}, "judge_load": {"max_workers": 3, "timeout": 600}, "expected_judge_calls": 10, "judge_calls": 14, "ragas_version": "0.4.3"}, "seconds": 74.17, "started_at": "2026-08-22 23:02:27", "notes": ["every LLM stage ran on gpt-5.6-luna via codex (effort=low)", "embedder sentence-transformers on heydariAI/persian-embeddings: Farsi + English (Persian-tuned)"], "selection": {"balance": "stride", "limit": 1, "n": 1, "by_difficulty": {"easy": 1}, "question_ids": ["mtg-001"]}}}}