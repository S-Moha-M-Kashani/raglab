"""Everything that is — or will equip — an LLM agent: `agentic_rag` (the
measured loop, inside the seam), `widget` (the panel's helper, outside it),
and `extra_tools` (terminal-only today, written to be handed to an agent)."""
