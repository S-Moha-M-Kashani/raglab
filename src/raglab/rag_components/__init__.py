"""The measured pipeline's stages: `indexing/` (what gets stored),
`retrieval/` (question → evidence), and `question_to_answer_pipeline` — one
question end to end, used by several components, so it sits at this level."""
