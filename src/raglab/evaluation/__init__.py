"""Scoring what the stages produced: the deterministic metrics, the RAGAS
judged metrics, the run harness, the shipped-assistant baseline preset, the
leaderboard that groups recorded runs before it ranks them, and evaluation's
own service — the experiment ledger."""
