"""The frontend: one served app on :9002 (`served_lab.py`, composing the panel
at `/` with the Inspector mounted at `/inspector`), the route plumbing and
presentation they share, their static pages in `frontend/`, and their
launchers in `cli/`."""
