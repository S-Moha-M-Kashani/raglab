__pycache__/
.venv/
*.pyc

# One JSON file per eval run — the lab's only durable artifact besides the
# ledger. The index, the retrieved contexts and the answers live in process
# memory and are discarded with it. Reproducible from the fixtures,
# machine-specific, appended to on every sweep.
.runs/

# Datasets imported through the panel. The four bundled samples in
# fixtures/corpus_groundtruth_datasets/ are part of the repository — they are reference
# points, and the argument for a finding being general rather than true of Farsi
# diaries. A corpus somebody uploaded is their material, not this repository's.
.datasets/

# The experiment ledger. Machine-local history, not source. The widget's
# conversation memory (databases/widget.db) is this repo's first WAL-mode
# SQLite user — its checkpointer runs `PRAGMA journal_mode=WAL`, which is
# where the `-shm` and `-wal` siblings come from; raglab.db uses a bare
# sqlite3.connect() and never produced them.
*.db
*.db-journal
*.db-shm
*.db-wal

# Safety copies of the ledger and runs, made before a destructive migration.
# Real work product, machine-local like the originals.
databases/backup-*/

# Tooling scratch, none of it source.
.claude
.superpowers/
.worktrees/
.pytest_cache/

# Notes, plans and reports. Kept on this machine, not in the repository.
docs/
.plans/
ARCHITECTURE.md

# One JSON file per judge screen — the evidence for which model was allowed to
# grade the deciding metrics. Machine-local, like the runs it qualifies.
.screens/
.env
docs/

# graphify's knowledge-graph output. Rebuilt from the source it describes,
# machine-local like the runs — kept out until it earns a place.
graphify-out/
.ruff_cache/
.DS_Store
